/*
	Use this file to boot up the system to test
*/

var configure = function () {
//     this.setDefaultTimeout(60 * 1000); //uncomment this to resolve processes that take more than 5 seconds
};

module.exports = configure;